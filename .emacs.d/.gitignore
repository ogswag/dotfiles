# Generated / machine-local state - nothing here belongs in the repo.

# All machine-local package/session state -- see the `user-emacs-directory'
# redirect in init.el (elpa, eln-cache, tree-sitter, transient,
# auto-save-list, eshell, recentf, savehist, save-place, project lists...).
local/

# Compilation output
*.elc

# Backups / autosaves, in case the "off" settings ever change
*~
\#*\#
.\#*

.DS_Store
